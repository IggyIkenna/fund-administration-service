# Coverage floor exception — fund-administration-service

**Status:** new service scaffold (2026-04-20).

The repo bootstraps with `MIN_COVERAGE=70`. This is a **starting baseline for a greenfield repo**, not an exception
to the general 80% target — further phases in
`unified-trading-pm/plans/active/fund_administration_service_and_pooled_subscription_redemption_2026_04_20.md`
ratchet coverage up as we move off the in-memory reference persistence store toward SQL/Firestore, wire in the real
`NavProvider`, replace the mock AML/KYC gate with the live adapter, and add the signed wire/fiat settlement legs.

Every subsequent PR must leave coverage >= the committed floor (enforced by
`unified-trading-pm/scripts/coverage-floor-guard.sh`).
